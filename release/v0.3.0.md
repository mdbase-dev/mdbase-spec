# mdbase v0.3.0 Ecosystem Release Contract

## Status

This is the auditable release contract for the stable mdbase `0.3.0` ecosystem.
It distinguishes required release behavior from useful follow-up work.

Current implementation version: `0.3.0-rc.2`.

Runtime profile version: `0.1.0`.

Stable `0.3.0` is not complete until every required gate below has evidence.

Local implementation qualification is complete and the protocol has been
promoted to the repository root. The second release candidate refines the
public specification boundary and is being published in dependency order.
Stable remains blocked on registry install verification, downstream
multi-platform CI, and live Obsidian smoke evidence.

## Ownership Decisions

### Collection Protocol

`mdbase-spec` owns the normative collection protocol, canonical schemas,
profile identifiers, conformance fixtures, and migration rules. Implementations
consume those artifacts; they do not redefine them.

The `mdbase-dev/mdbase.dev` repository owns the ecosystem website and its
deployment cadence. It imports the versioned specification reader built here;
a specification tag no longer deploys or replaces the product website.

### TypeScript Runtime Package

Generic runtime contracts and the reference in-memory provider host belong in a
small browser-safe package in the mdbase package family. The target package name
is `@callumalpass/mdbase-runtime`, versioned with runtime profile `0.1.x`.

The package owns:

- canonical runtime types and schemas
- provider registration and removal
- deterministic contract composition
- preflight and capability-policy checks
- event-envelope validation and delivery
- action input/output validation and dispatch
- platform-neutral host lifecycle
- exact canonical schemas for the declared collection and runtime profile
  versions

Node-specific Markdown loading and materialization, if shipped in the same
package, MUST live behind an explicit `./node` export. Importing the main package
MUST NOT load Node APIs, SQLite, Obsidian APIs, or TaskNotes code.

Schema access APIs MUST return immutable values or isolated copies. Consumers
MUST be able to validate an in-memory provider from the installed package
without a source checkout or collection filesystem.

### Rust Runtime Contract Harness

The v0.3.0 release includes `mdbase-runtime-contracts-rs` as an independent
contract-validation harness. It loads the same canonical schemas and example
collections as the TypeScript runtime package and validates type files,
contract records, and embedded action and event schemas.

The harness may remain smaller than a runtime host. Its release role is to
exercise the shared contract model from Rust and catch language-specific
assumptions in the schemas and fixtures.

These TypeScript and Rust package commitments are ecosystem release
requirements. Runtime Contracts conformance remains defined by the portable
profiles and permits independent implementations and package structures.

### Platform Host Discovery

The protocol does not prescribe global process or plugin discovery. Platform
adapters inject or broker one selected host instance.

For Obsidian, the `mdbase-obsidian` plugin is the selected host adapter. It
exposes the host through its plugin API and owns host disposal with the vault
lifecycle. Independent provider and executor plugins discover that API by the
`mdbase-obsidian` plugin ID. They MUST NOT use TaskNotes as the generic service
locator. A temporary TaskNotes compatibility host MAY exist during alpha
migration, but it MUST be removed before the release candidate.

### Providers And Executors

TaskNotes and Canvas Bases are optional providers. Neither owns the generic
runtime contract. TaskNotes Workflows is an optional executor and may remain a
TaskNotes-focused product, but generic event/action preflight and dispatch use
the mdbase runtime package.

Canvas Bases MUST remain fully usable without TaskNotes. TaskNotes-specific
actions are available only when the `tasknotes` provider is registered and
passes preflight.

## Required Component Matrix

| Component | Stable release responsibility | Required claim or evidence | Dependency rule |
| --- | --- | --- | --- |
| `mdbase-spec` | normative prose, schemas, fixtures, migration rules | all artifact and site gates | source of truth |
| `@callumalpass/mdbase-runtime` | portable contracts and reference host | `runtime_contracts/0.1`; browser package smoke | no TaskNotes/Obsidian/Node dependency in main export |
| `mdbase-runtime-contracts-rs` | independent runtime contract validation harness | canonical schema, example collection, and embedded-schema tests | consume the same versioned artifacts as the TypeScript runtime package |
| `@callumalpass/mdbase` | TypeScript collection implementation | `core_read`, `collection_semantics`, `cel`, `cel_query`, `links`, `core_write`, `lifecycle` | exact compatible spec artifacts |
| `mdbase-cli` | safe user-facing operations and migration | installed-tarball suite and v0.2 migration rehearsals | publish after TypeScript core |
| `mdbase-rs` | independent Rust collection implementation and separately versioned workflow engine | `core_read`, `collection_semantics`, `cel`, `cel_match`, `cel_query`, `links`, `core_write`, `lifecycle`, `runtime_contracts/0.1`, `watch`; workflow execution remains separately evidenced | claim only verified profiles per artifact |
| `mdbase-lsp` | editor diagnostics from Rust semantics | Rust claim plus LSP diagnostic fixtures | no independent schema dialect |
| `mdbase-obsidian` | selected Obsidian host adapter and collection UI | shared-host, unload, and provider-discovery smoke tests | owns one generic host per loaded vault |
| `@tasknotes/model` | TaskNotes domain types and provider metadata | ESM/CJS package smoke | generic runtime types come from mdbase runtime package |
| TaskNotes | `tasknotes` provider and domain operations | provider contract, dispatch, event, authorization tests | may host only through generic adapter |
| TaskNotes Workflows | workflow executor and TaskNotes integrations | canonical file generation, legacy migration, registration, policy, and dispatch tests | runtime host is injected, not TaskNotes-owned |
| Canvas Bases | `canvas-bases` provider and `canvas.drop` event | works without TaskNotes; cross-plugin workflow smoke | TaskNotes integration is optional |
| Pickle and `.ops` | representative migrated consumers | checked migration plus collection validation | no silent rewrite |

## Stable Release Gates

### Specification

- [x] No unresolved normative questions in chapters 00-16.
- [x] Every canonical schema validates as Draft 2020-12.
- [x] Conformance claims validate against the canonical claim schema.
- [x] Examples and migration fixtures are generated or checked reproducibly.
- [x] The promoted site and archived v0.2 documentation both build.

### Runtime Ownership

- [x] The generic TypeScript runtime package is browser-safe and publishable.
- [x] Its main export carries immutable or isolated canonical schemas and the
      reference host enforces them for live providers and event envelopes.
- [x] TaskNotes model, TaskNotes Workflows, and Canvas Bases use its canonical
      types rather than duplicate generic interfaces.
- [x] The reference host implementation no longer lives in TaskNotes.
- [x] `mdbase-obsidian` exposes the selected host and provider discovery works
      without TaskNotes installed.
- [x] Canvas Bases emits `canvas.drop` and runs generic workflows without
      TaskNotes.
- [x] TaskNotes actions appear and dispatch only when its provider is available.
- [x] TaskNotes Workflows writes canonical `workflow/0.1` records, registers
      them with the selected host, and keeps legacy files read-only until an
      explicit backed-up migration is approved.

### Data Safety And Compatibility

- [x] v0.2 collections remain readable by transition-capable tools.
- [x] Migration analysis is non-mutating and reproducible.
- [x] Apply requires explicit approval and verifies source hashes.
- [x] Backups, rollback, interruption, stale-report, invalid-record, and partial
      migration behavior have fault-injection coverage.
- [x] TaskNotes, Pickle, and one non-Obsidian collection complete migration
      rehearsals from copied real-world data.

### Implementations

- [ ] At least one adapter passes every required core profile fixture.
- [x] TypeScript and Rust include validated conformance claim documents.
- [x] A second implementation passes shared `core_read` and
      `collection_semantics` fixtures.
- [x] LSP diagnostics preserve canonical codes, paths, fields, and schema
      locations.
- [x] Unsupported profiles and operational limits are explicit.

### Release Qualification

- [ ] Linux, macOS, and Windows CI pass for publishable packages.
- [ ] Fresh tarball/crate installs pass without sibling links.
- [x] Production and development dependency audits have no known high or
      critical findings.
- [ ] Live Obsidian smoke tests pass for TaskNotes, Workflows, and Canvas Bases.
- [x] Performance baselines pass on representative small and large collections.
- [x] Upgrade, rollback, provider-authoring, and workflow-authoring documentation
      is complete.

## Qualification Evidence

Evidence collected through 2026-07-19 in Linux x86_64:

| Surface | Result |
| --- | --- |
| v0.3 artifact suite | 36 executable and 89 adapter-target cases pass |
| conformance claims | TypeScript and Rust claims validate against the canonical schema |
| TypeScript core | build and all 1,939 tests pass, including stable-marker and retained alpha-marker compatibility |
| CLI | 265 tests pass, including init, analyzed migration, apply, recovery, stale reports, and partial policy |
| Rust core | fmt, strict clippy, full tests, shared v0.3 fixture, stable-v0.3 init safety, v0.3 profiler smoke, and crate packaging pass |
| LSP | fmt, strict clippy, and 45 tests pass; crate verification waits for the registry core crate |
| runtime package | 20 host/contract tests and browser-entry smoke pass |
| runtime executor / CEL / Rust contract harness | 2 / 6 / 2 tests pass |
| TaskNotes | 3,906 tests plus lint, architecture, typecheck, docs, build, test-vault copy, and 7 performance tests pass; real-host coverage includes registration, unload, events, policy denial, dispatch, and schema validation; live Obsidian smoke remains pending |
| TaskNotes Workflows | 49 tests, 600/600 translation keys in all supported locales, lint, typecheck, production build, and test-vault copy pass; live Obsidian smoke remains pending |
| Canvas Bases | 88 tests plus lint, typecheck, and build pass without a TaskNotes dependency |
| Pickle Obsidian | 63 tests plus lint, typecheck, and build pass |
| mdbase-tasknotes | 1,022 tests pass against the v0.3 TypeScript core and v0.3 TaskNotes model |
| additional consumers | Pickle Rust, Pickle Android, workout, mdbase-app, Tauri, and tasknotes-tui checks pass; Rust consumers are fmt/clippy-clean where configured |
| dependency audit | no production findings and no high/critical development findings; only current low-severity esbuild findings remain in affected dev toolchains |
| core performance | 2,001 tasks, 80 projects, 100 rename references: basic query p95 92.5 ms, formula query p95 99.5 ms, update p95 90.7 ms, create p95 100.4 ms, cache rebuild p95 271.5 ms |
| site | current v0.3 and archived v0.2 specification readers build from required file lists and are imported by the independent website |

The in-app browser bridge and Obsidian CLI attachment were unavailable in this
environment. Build success is not a substitute for those live visual/runtime
smoke gates.

## Publication Order

1. Tag the exact spec, schemas, fixtures, and runtime profile artifacts.
2. Publish `@callumalpass/mdbase-runtime`.
3. Publish `@callumalpass/mdbase`.
4. Publish `mdbase-cli` and the Rust crates/binaries.
5. Publish `@tasknotes/model` against the runtime package, then publish
   `mdbase-tasknotes` against the registry core and model packages.
6. Release `mdbase-obsidian` as the selected Obsidian host adapter.
7. Release TaskNotes, then TaskNotes Workflows and Canvas Bases.
8. Validate downstream collections before declaring the ecosystem release
   complete.

Every dependent lockfile MUST resolve the published prerelease or stable
artifact, not a sibling link or an older compatibility branch, before its
release is cut.

The exact upgrade, recovery, provider, workflow, and staged dependency commands
are in [v0.3.0-operator-guide.md](./v0.3.0-operator-guide.md).

## Downstream Migration Order

After the specification artifacts are tagged and the required packages are
published, downstream migration proceeds in this order:

1. shared schemas and runtime contract helper packages
2. TypeScript and Rust core readers
3. CLI validation and migration dry-run
4. LSP schema and workflow diagnostics
5. TaskNotes generated export
6. TaskNotes workflow and runtime projects
7. Canvas Bases runtime workflow integration
8. Pickle and other mdbase-backed applications

Generated type files migrate before user-authored vault files. Each stage uses
the published artifacts and validated output from its prerequisites.

## Prerelease Progression

- Alpha releases may change schemas and package boundaries.
- Beta begins only after normative schemas and public exports freeze.
- Release candidate begins only after required consumers use the generic host.
- Stable `0.3.0` requires every gate in this document to have durable evidence.
